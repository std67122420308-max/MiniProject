[project]
name = "miniproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask-migrate>=4.1.0",
    "gunicorn>=25.1.0",
    "psycopg2-binary>=2.9.11",
]
